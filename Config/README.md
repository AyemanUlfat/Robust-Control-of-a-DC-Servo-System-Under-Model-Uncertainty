# Config

YAML files that define plant parameters, controller gains, simulation settings, and Monte Carlo options.

| File | Purpose |
|------|---------|
| `default.yaml` | Parameters used in the final report |

Edit `default.yaml` instead of hard-coding numbers in Python. Source code loads this file at runtime.

Do not put secrets here. This project has none.
